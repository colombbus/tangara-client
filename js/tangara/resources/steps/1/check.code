//learn.dumpStatements();
learn.wait(1000);
if (learn.hasStatement({expression: { left:{name: 'bob'}, operator:'=', right:{type:'NewExpression', callee:{name: 'Robot'}}}})) { 
    learn.validateStep();
} else if (learn.hasStatement({expression: { operator:'=', right:{type:'NewExpression', callee:{name: 'Robot'}}}})) { 
    learn.invalidateStep("Pas mal, mais ce serait mieux s'il s'appelait bob !");
}
